services:
  vienna-roof-explorer:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_DEBUG: "0"
      HOST: "0.0.0.0"
      PORT: "5000"
      RUNTIME_DATA_DIR: "/app/data/runtime"
    restart: unless-stopped
